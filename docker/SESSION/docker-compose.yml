name: SESSION

services:
  redis-session:
    image: redis:7.2
    container_name: redis-session
    ports:
      - "6379:6379"
    command: ["redis-server", "--requirepass", "redis1234"]
    restart: unless-stopped
